webpackJsonp([0x8f64bf10dfd8],{542:function(e,n){e.exports={pathContext:{posts:[{html:'<h2>Public and Private Keys in OpenSSL</h2>\n<p>Here is how to generate a private key with a size of 2048 bit:</p>\n<pre><code class="language-bash">openssl genrsa -out nicolas-rsa.pem 2048\n</code></pre>\n<p>Here is how to decompose the generated privated key:</p>\n<pre><code class="language-bash">openssl rsa -text -in nicolas-rsa.pem -noout\n</code></pre>\n<p>This displays the elements of the private key: modulus, publicExponent, privateExponent, prime1, prime2, exponent1, exponent2 and coefficient.</p>\n<p>Here is how to get the public key:</p>\n<pre><code class="language-bash">openssl rsa -in nicolas-rsa.pem -pubout -text\n</code></pre>\n<p>Here is how to get the public key writen in a file:</p>\n<pre><code class="language-bash">openssl rsa -in nicolas-rsa.pem -pubout -out nicolas-rsa-pub.pem\n</code></pre>\n<p>Let us write a message into a file:</p>\n<pre><code class="language-bash">echo -n \'this is a message\' > file.txt\n</code></pre>\n<p>This is how to encrypt this file with our public key:</p>\n<pre><code class="language-bash">openssl pkeyutl -encrypt -inkey nicolas-rsa-pub.pem -pubin -in file.txt -out encrypted-file.txt\n</code></pre>\n<p>This is how to now decrypt the encrypted file with the private key:</p>\n<pre><code class="language-bash">openssl pkeyutl -decrypt -inkey nicolas-rsa.pem -in encrypted-file.txt -out result.txt\n</code></pre>\n<p>If we want to use elliptic cryptography instead of rsa as the algorithm, let us display all of the possible types of curves:</p>\n<pre><code class="language-bash">openssl ecparam -list_curves\n</code></pre>\n<p>A prime256v1 curve is a standard option. Let us generate a private key with this option:</p>\n<pre><code class="language-bash">openssl ecparam -genkey -name prime256v1 -out ecc-key.pem\n</code></pre>\n<p>With an additional -noout option, the elliptic curve parameters are not included in the file:</p>\n<pre><code class="language-bash">openssl ecparam -genkey -name prime256v1 -noout -out ecc-key.pem\n</code></pre>\n<p>About the pem file extension: pem means "Privacy Enhanced Mail".\nA pem file may contain almost anything base64 encoded and wrapped with BEGIN and END lines.</p>\n<p>About the der file extension: DER means Distinguished Encoding Rules. Data in der files (for instance X.509 certificates or private keys) are in binary format. DER files do not contain human readable plain text.</p>\n<p>Here is how to print elements about our ecc key :</p>\n<pre><code class="language-bash">openssl ec -in ecc-key.pem -text\n</code></pre>\n<p>This is how to save the public key in a file: </p>\n<pre><code class="language-bash">openssl ec -in ecc-key.pem -pubout -out ecc-pub.pem\n</code></pre>\n<h2>Creating a root certificate</h2>\n<p>A root certificate and the associated private key can be created with:</p>\n<pre><code class="language-bash">openssl req -x509 -sha256 -days 365 -key ecc-key.pem -out ecc-cert.pem\n</code></pre>\n<p>and then displayed with:</p>\n<pre><code class="language-bash">openssl x509 -in ecc-cert.pem -text -noout\n</code></pre>\n<p>Here is how to create a certificate using a configuration file called certificate.conf\n(and we use the private key nicolas-rsa.pem):</p>\n<pre><code class="language-bash">openssl req -x509 -days 365 -key nicolas-rsa.pem -config certificate.conf -out cert-config.pem\n</code></pre>\n<pre><code class="language-bash">openssl x509 -in cert-config.pem -text -noout\n</code></pre>\n<p>The same certificate can be created without any prompting using a conf file stating <code>prompt = no</code>:</p>\n<pre><code>countryName = US\nstateOrProvinceName = California\nlocalityName = San Francisco\norganizationName = Nicolas Ltd\norganizationalUnitName = Information Department\ncommonName = nicolas-ltd.com\nemailAddress = info@nicolas-ltd.com\n\n[ v3_extensions ]\nsubjectKeyIdentifier = hash\n# copies the subject key identifier (or if it fails then it uses the issuer and serial number)\nauthorityKeyIdentifier = keyid, issuer\n# whether the certificate is a root or not\nbasicConstraints = critical, CA:true, pathlen:2\n# key usage- use the certificate for signing or key encipherment\nkeyUsage = digitalSignature, keyEncipherment\n</code></pre>\n<p>Here is the command to create the certificate:</p>\n<pre><code class="language-bash">openssl req -x509 -days 365 -key nicolas-rsa.pem -config certificate_no_prompt.conf -out cert-config.pem\n</code></pre>\n<p>and here is how to display the certificate thus created:</p>\n<pre><code class="language-bash">openssl x509 -in cert-config.pem -text -noout\n</code></pre>\n<h2>Creating a Certificate Signing Request</h2>\n<p>Here is how to generate and then display the CSR:</p>\n<pre><code class="language-bash">openssl req -key ecc-key.pem -new -out ecc-inter-cert-csr.pem -config cert-csr.conf\n</code></pre>\n<pre><code class="language-bash">openssl req -in ecc-inter-cert-csr.pem -text\n</code></pre>\n<p>Given the private key <code>ca-key.pem</code> generated by the root authority with command:</p>\n<pre><code class="language-bash">openssl genrsa -out ca-key.pem 2048\n</code></pre>\n<p>Here is how the root authority can generate and display the root certificate:</p>\n<pre><code class="language-bash">openssl req -x509 -sha256 -days 365 -key ca-key.pem -out ca-root-cert.pem -config certificate.conf\n</code></pre>\n<pre><code class="language-bash">openssl x509 -in ca-root-cert.pem -text -noout\n</code></pre>\n<p>Here is how the authority can create the signed intermediate certificate:</p>\n<pre><code class="language-bash">openssl x509 -req -CA ca-root-cert.pem -CAkey ca-key.pem -in ../ecc-inter-cert-csr.pem -out intermediate-cert.pem -days 365\n</code></pre>\n<p>In case there are extensions in the CSR to include, a <code>-copy_extensions=copyall</code> option has to be added to the same command. The signed intermediate certificate can be displayed with:</p>\n<pre><code class="language-bash">openssl x509 -in intermediate-cert.pem  -text -noout\n</code></pre>\n<h2>Verifying the chain of trust</h2>\n<p>Here is how to verify the signature of the intermediate certificate using the public key of the root certificate:</p>\n<pre><code class="language-bash">openssl verify -CAfile ca-root-cert.pem intermediate-cert.pem\n</code></pre>\n<p>The v3 extensions of the root certificate need to include the <code>keyCertSign</code> value for the keyUsage extension, e.g. </p>\n<pre><code>keyUsage = keyCertSign, digitalSignature, keyEncipherment\n</code></pre>',id:"/home/nicolas/projects/workshop/src/pages/2026-03-25-openssl/index.md absPath of file >>> MarkdownRemark",frontmatter:{date:"2026-03-25T21:54:00.000Z",path:"/openssl",title:"OpenSSL",excerpt:"",tags:["openssl"]}}],tagName:"openssl"}}}});
//# sourceMappingURL=path---tags-openssl-32acf37e6731d5f66126.js.map